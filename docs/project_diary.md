# Дневник проекта: Магический магазин (Magic Shop Tycoon)

> Документ обновляется в ходе разработки. Фиксирует ключевые решения, архитектуру, состояние механик и приоритеты оптимизации.

---

## 1. Стек и фундаментальные правила
- **Платформа**: Яндекс Игры (Yandex SDK v2).
- **Стек**: Svelte + TypeScript + Vite + GSAP.
- **Графика**: Строго 100% чистый SVG (никаких растровых картинок и сторонних шрифтов).
- **Платформы**: Мгновенная адаптивность под десктоп и мобильные устройства.
- **Локализация**: Полный перевод на 3 ключевых языка комьюнити (Русский, Английский, Турецкий).
- **Звук**: Web Audio API (синтез процедурных звуковых эффектов на осцилляторах без внешних mp3).
- **Ограничения агента**: Строго запрещен `browser_subagent`. Визуальная проверка — Chrome headless через PowerShell.

---

## 2. Ключевые системы и механики
- **Котел**: Клик/тап, термодинамика перегрева (Heat gauge & Combo multiplier), тактильный GSAP squash & stretch.
- **Экономика**: Золото, Кристаллы, Звездная пыль (Stardust), 15+ рецептов алхимии, ингредиенты 4 редкостей.
- **Прогресс**: Ребирты (Перерождения) с древом талантов за звездную пыль.
- **Питомцы**: 6 уникальных спутников с пассивными аурами, прокачкой уровней и экспедициями.
- **Монетизация**:
  - Банк (Стартер пак, наборы кристаллов за Яны через Yandex Payments SDK).
  - Бесплатный ежедневный подарок в банке.
  - Rewarded Video (Дар летящей искры, пропуск таймеров, умножение офлайн-дохода).
  - Календарь наград: 7-дневный цикл с недельными вехами.
  - Interstitial реклама при закрытии модалок с тайм-аутом антиспама.

---

## 3. Выполненные этапы (Хронология)
1. **Redesign Starter Pack & Bank Modal**: Премиальное золото/рубиновое оформление, таймер выгоды x4, бейджи бестселлеров (`822dda9`).
2. **Mobile Touch Targets**: Увеличение всех зон клика (close buttons, bottom dock, icon buttons) до строгих >=44x44px (`a323342`).
3. **Daily Calendar Overhaul**: 7-дневный непрерывный цикл, полоса недельного прогресса, гарантированный сундук 7-го дня (`a31600b`).
4. **Haptic & Tactile Juice**: Эластичные отскоки котла, импульсы прокачки в магазине, вибро-шейк при нехватке золота (`9e535d4`).
5. **Yandex Build & Security**: Zero-404 бандл `magic-shop-tycoon-yandex.zip`, валидация времени сервера против манипуляций часами, защита от автокликеров (ClickRateLimiter).
6. **Mobile 60 FPS Engine Overhaul**:
   - `ParallaxBackground.svelte`: замена `feGaussianBlur` на аппаратные радиальные градиенты GPU; перенос всплывающих пузырей с 24-64 JS-тикеров GSAP на чистый CSS Compositor (`translate3d`).
   - `Cauldron.svelte`: ликвидация 15 SMIL `<animate>` тегов; переход на аппаратные CSS-кейфреймы пузырей; оптимизация вылетающих цифр через `-webkit-text-stroke` вместо 40 проходов `text-shadow`.
   - `PetCompanion.svelte`: удаление невалидного фильтра размытия, внедрение шейдерной ауры и аппаратного покачивания/дыхания без JS-нагрузки.
   - `FlyingBonus.svelte`: удаление двойной фильтрации на летящей сущности.
   - `App.svelte`: отключение `backdrop-filter` на плавающих баффах на мобильных экранах.
   - Результат: 0ms фонового JS-тика на анимациях, разгрузка потока отрисовки, стабильные 60 FPS.
7. **Harmonized Arcanum Modal Mobile Opening**:
   - Окно «Арканум» (`PremiumModal.svelte`) переведено на единый мобильный стандарт (`@media (max-width: 680px)`): полноэкранный режим `100dvh`, нулевые отступы оверлея, отключение размытия подложки, синхронизированный `z-index: 1000`. Полное единообразие со всеми модальными окнами главного хаба (`545b78a`).
8. **GameplayAPI Event Deduplication**:
   - В `src/yandex-sdk.ts` внедрен внутренний трекинг активности геймплея `isGameplayActive`. Исключены избыточные postMessage-сообщения `gameplay_api_start` при частых кликах и переключениях фокуса окна; консоль и связь с контейнером Яндекса очищены от спама событий.
9. **Mobile Chest Loot Grid Responsive Overhaul**:
   - В `ChestShopModal.svelte` исправлена фаза выдачи наград (`phase === 'loot'`):
     * Брейкпоинт синхронизирован с остальными модалками (`@media (max-width: 680px)`).
     * Обнулены внешние отступы подложки `.overlay` и `.cinematic-overlay` (устранено 80px лишних невидимых отступов).
     * Сетка наград `.loot-grid` переведена на `repeat(3, minmax(0, 1fr))` со строгим `box-sizing: border-box`, `min-width: 0`, `overflow: hidden` у карточек — 3 колонки идеально помещаются в ширину любого мобильного экрана без горизонтального срезания.
     * Для ультра-узких экранов (<=340px) предусмотрен адаптивный фоллбэк в 2 колонки.
     * Кнопка «Забрать в инвентарь» закреплена внизу (`position: sticky`), доступна без обязательного скролла длинного списка.
10. **Mobile Daily Quests Tab Responsive Overhaul**:
   - В `DailyQuestsModal.svelte` устранена проблема сильного сжатия и обрезки текста заданий на мобильных экранах (`@media (max-width: 680px)`):
     * Карточка квеста `.quest-card` перестроена с плоской 1-строчной структуры (где 5 элементов сжимали название задачи до 3-4 букв) на эргономичную двухстрочную компоновку:
       1. Верхняя строка: иконка (40px/34px), бейдж сложности (`легкий`/`средний`/`сложный`), полное переносимое название задания (`word-break: break-word; flex-wrap: wrap`) и компактная плашка награды (золото/кристаллы).
       2. Нижняя строка: вытянутый прогресс-бар с моноширинным счетчиком `X / Y` и кнопка сбора / статус выполнения.
     * Сохранены все Svelte-биндинги `bind:this` для GSAP-анимаций прогресса и интерактивности кнопок сбора.
     * Добавлены адаптивные стили для баннера гильдии, карточки «Ларец Мастера Дня» и фоллбэки для ультра-узких экранов (<=380px).
11. **Mobile Alchemy Recipe Book Accessibility & Responsive Overhaul**:
   - В `AlchemyTableModal.svelte` решена проблема недоступности и скрытия «Книги Рецептов» на смартфонах:
     * **Причина**: На экранах `<=640px` сетка сворачивалась в 1 колонку, в результате чего котёл, 3 слота, кнопка варки и инвентарь ингредиентов занимали всю высоту экрана (>600px), оттесняя книгу рецептов глубоко за пределы видимости. Дополнительно возникал конфликт двойной прокрутки из-за `overflow-y: auto` внутри `content-scroll`.
     * **Мобильные подвкладки (`@media (max-width: 680px)`)**: Внедрена быстрая сегментированная навигация в шапке алхимии: `[ ⚗️ Котёл ]` и `[ 📖 Книга Рецептов (X/14) ]`.
     * **Быстрый переход**: На панели котла добавлен баннер прямого перехода `📖 Открыть Книгу Рецептов (X/14) →`, а внутри книги — обратная кнопка `← К котлу`.
     * **Ликвидация конфликта прокрутки**: На мобильных экранах для `.recipe-book` установлено `overflow-y: visible; max-height: none`, список рецептов прокручивается нативно и плавно.
     * **Десктоп не затронут**: На экранах `>680px` подвкладки скрыты (`display: none`), панели котла и книги отображаются одновременно в две колонки (`grid-template-columns: 1fr 1fr`).
12. **Mobile Fraction & Slash Line-Wrap Elimination**:
   - Проведено комплексное исправление некрасивых переносов строк вокруг символа слэша `/` (например, `35% /` на первой строке и `100%` на второй) по всему интерфейсу игры:
     * `ChestShopModal.svelte`: шкала астрального резонанса (`.resonance-percent`) переведена на `white-space: nowrap; flex-shrink: 0`, в шаблоне использован неразрывный пробел `&nbsp;/&nbsp;`, заголовок шкалы защищен от выталкивания (`overflow: hidden; text-overflow: ellipsis; min-width: 0`).
     * `DailyQuestsModal.svelte`: счетчики контрактов дня (`.completed-summary`, `.mastery-counter`) и шкалы выполнения заданий (`.progress-label`) защищены через `white-space: nowrap; flex-shrink: 0`.
     * `CustomerOrders.svelte`: бейдж активных заказов (`.orders-count-badge`) и счетчики нужных ингредиентов (`.req-qty`) защищены от разбиения.
     * `CollectionsTab.svelte`: числовой прогресс реликвий витрин (`.progress-nums`) переведен на `white-space: nowrap; flex-shrink: 0`.
     * `ArtifactStore.svelte`: синергетические счетчики коллекций (`.synergy-count`) и бейджи вкладок (`.tab-badge`) зафиксированы на одну строку.
     * `FamiliarsModal.svelte`: счетчик собранных питомцев (`.sub-pill`) защищен от переноса.
     * `DailyCalendarModal.svelte`: индикатор недельного прогресса (`.week-progress-pill`) защищен от переноса.
     * `OfflineIncomePopup.svelte`: таймер и вместимость офлайн-хранилища (`.storage-values`) переведены на неразрывный пробел и `nowrap`.
     * `InventoryModal.svelte`: бейджи уровней мастерства зелий (`.mastery-badge`, `.mastery-xp`) зафиксированы от переносов.
     * `AlchemyTableModal.svelte`: индикаторы остатка попыток до перегрева (`.danger-label`) и шаги подсказок (`.hint-step-tag`) защищены от переносов.
     * `ShopModal.svelte`: прогресс ранговых рубежей магазина (`.milestone-step`) защищен от переносов.
     * `GrimoireModal.svelte`: бейджи вкладок гримуара (`.tab-badge`) зафиксированы на одну строку.
13. **Mobile Dragon Gift Card Alignment & Centering Overhaul**:
   - В `BankModal.svelte` исправлена асимметрия карточки «Дар Дракона» на мобильных устройствах:
     * **Причина**: В `@media (max-width: 520px)` карточка переключалась в колоночный режим `flex-direction: column; text-align: center;`. При этом `.gift-title-row` (содержащий название «Дар Дракона» и бейдж «БЕСПЛАТНО») являлся flex-контейнером со стандартным `justify-content: flex-start`, из-за чего заголовок прижимался к левому краю, в то время как иконка сверху, описание и кнопка забора кристаллов центрировались.
     * **Синхронизация брейкпоинта**: Брейкпоинт мобильной адаптации в `BankModal.svelte` синхронизирован с `PremiumModal.svelte` до `@media (max-width: 680px)`.
     * **Полная центровка элементов**: Добавлено правило `.gift-title-row { justify-content: center; }`, а также задано явное центрирование для блока действия (`.gift-action`), кнопки (`.claim-gift-btn { max-width: 220px; justify-content: center; }`) и таймера кулдауна (`.gift-cooldown-badge { margin: 0 auto; }`).
14. **Daily Quests Mastery Card Mobile Layout Overhaul**:
   - В `DailyQuestsModal.svelte` и `CityModal.svelte` устранена асимметрия и кривая компоновка карточки «Ларец Мастера Дня» на смартфонах:
     * **Причина**: На экранах `<=380px` срабатывало устаревшее правило `flex-direction: column; align-items: flex-start; .mastery-action { align-self: flex-end; }`, из-за чего иконка висела одна слева сверху, название и описание оказывались под ней, а плашка наград прижималась к правому нижнему углу, создавая разорванный и перекошенный вид.
     * **Перевод на 2-строчный CSS Grid**: На мобильных устройствах (`@media (max-width: 680px)`) карточка переведена на сетку `"icon details" / "action action"`:
       - Верхняя строка: иконка ларца слева (40x40px) в паре с заголовком «Ларец Мастера Дня» и счетчиком `X / 6` справа (`justify-content: space-between`), под ними описание;
       - Нижняя строка: вытянутая аккуратная центрированная плашка наград `[ 💎 +15  👑 Деревянный ларец ]` или полноразмерная кнопка забора `[ Забрать супер-приз ]`, занимающая всю ширину без выталкивания и обрезания текста.
     * **Устранение оверфлоу в CityModal**: В `CityModal.svelte` навигационная панель переведена на `repeat(4, minmax(0, 1fr))`, а для `.embedded-modal` в `DailyQuestsModal` заданы `padding: 0 !important; height: auto !important;`, что предотвратило раздувание модалки за пределы экрана на узких дисплеях.
15. **Integration of Meta-Skill «Task-Observer» (One Skill to Rule Them All)**:
   - В `.agents/skills/task-observer/` интегрирован мета-навык непрерывного наблюдения и улучшения базы навыков (Rebelytics):
     * Позволяет агенту автоматически улавливать замечания пользователя, пробелы в навыках и повторяющиеся рутинные паттерны.
     * Формирует структурированные наблюдения в `skill-observations/observation-log/` и превращает их в новые правила и навыки для проекта.
     * Образует триаду эффективности разработки: **Headroom** (сжатие токенов на лету) + **Claude-Mem** (долговременная память) + **Task-Observer** (эволюция навыков и правил).
16. **Archmage Lucky Wheel Monetization & Retention Feature («Колесо Фортуны Архимага»)**:
    - Разработана и интегрирована флагманская механика монетизации и удержания игроков:
      * **8 сбалансированных секторов**: Золото (30 мин дохода), 3 Кристалла, Алхимический буст x3 на 3 минуты, Редкий ларец, Пакет трав (+5 редких трав), Темпоральный скачок (1 час дохода), 10 Кристаллов и ДЖЕКПОТ (3 часа дохода + 25 кристаллов).
      * **Многоуровневая монетизация**:
        1. Бесплатный ежедневный спин раз в 24 часа для стимулирования возврата игроков (Day-1 / Day-7 retention).
        2. До 5 спинов за просмотр Rewarded Video в сутки с 30-минутным кулдауном антиспама (Yandex SDK v2).
        3. Платные спины за премиальные кристаллы: одиночный спин за 10 💎 и оптовый спин x10 за 90 💎 со скидкой 10%.
      * **Pity-система (Шкала Удачи Архимага 0/10)**: каждое 10-е вращение гарантированно приносит игроку бонусный Астральный Сундук, защищая от неудач и стимулируя покупку паков вращений.
      * **Чистый векторный дизайн (Pure SVG)**: Колесо построено на точной SVG-геометрии (8 секторов по 45°), радиальной ориентации иконок/подписей к центру, физическом отклонении указателя (wobble) и плавной анимации остановки `cubic-bezier(0.12, 0.8, 0.2, 1)`.
      * **Процедурный Web Audio саунд-дизайн**: реалистичные щелчки стрелки по штифтам синтезируются в реальном времени через AudioContext без внешних mp3/wav; победные фанфары при выигрыше джекпота.
      * **Полная адаптивность (Desktop & Mobile <=390px)**: кластер иконок верхнего HUD плавно сжимается на узких экранах смартфонов без горизонтального переполнения; диалог колеса отцентрирован с сохранением безопасных зон.

17. **Lucky Wheel Fixes & Svelte 5 Duplicate Key Bugfix**:
    - В `LuckyWheelModal.svelte` исправлена локализация: все названия наград внутри ячеек колеса фортуны и подписи к кнопкам переведены на RU/EN/TR через словарь `$t()`.
    - Устранена тряска колеса фортуны при вращении: убраны горизонтальные сдвиги от появляющегося ползунка прокрутки, контейнер колеса зафиксирован (`overflow: hidden; contain: layout`).
    - В `App.svelte` исправлена критическая ошибка Svelte 5 `each_key_duplicate` («Keyed each block has duplicate key `frenzy_lucky_wheel`»): ключи активных баффов дополнены уникальным идентификатором.
18. **Mobile Daily Calendar Modal Responsive Overhaul («Календарь Архимага»)**:
    - В `DailyCalendarModal.svelte` полностью устранена проблема обрезки карточек и невидимости дней на экранах смартфонов (320px–420px):
      * **Корень проблемы**: `.calendar-scroll-area` имела `display: flex; flex-direction: column`. Без `flex-shrink: 0` флексбокс сжимал каждую из 4 секций недель `.week-section` до ~128px, а правило `overflow-x: hidden` на неделях вызывало расчет `overflow-y: auto`, создавая паразитные вложенные скроллбары и скрывая все карточки со 2-го по 4-й ряд (дни 4–7, 11–14, 18–21, 25–30).
      * **Базовые стили и каскад**: Базовые стили карточек `.day-card`, меток наград и кнопок перенесены выше медиа-запросов, восстановив правильную специфичность CSS.
      * **Устранение сжатия**: На `.week-section` задан `flex-shrink: 0;` и удален `overflow-x: hidden`, благодаря чему недели разворачиваются на полную естественную высоту, а плавный вертикальный скролл осуществляется единым главным контейнером `.calendar-scroll-area`.
      * **Эргономичная мобильная сетка**: На экранах `<=500px` сетка дней перестроена на 3 колонки (`repeat(6, minmax(0, 1fr))` со `span 2`), карточки вех недели (День 7, День 14, День 21, День 30) занимают всю ширину (`1 / -1`) в виде роскошных горизонтальных баннеров с аурой.
      * **Поддержка ультра-узких экранов (<=360px)**: Компактные отступы, масштабированные иконки и шрифты без переносов и срезания текста.
      * **Фиксация фона в App.svelte**: Добавлен `:global(html) { overflow-x: hidden; }` и `:global(body) { max-width: 100vw; overflow-x: hidden; }`, исключающий горизонтальное расширение вьюпорта от фоновых SVG-частиц.

19. **Archmage Calendar Ergonomics & Polish Overhaul («Календарь Архимага: Эргономика и Ликвидация Багов»)**:
    - **Исправление бага с фамильяром Дня 21 (Пегас)**: В `calendar.ts` и `DailyCalendarModal.svelte` устранено жесткое зашивание Совы вместо Пегаса (`rewardPegasus`, `rewardPegasusDesc`); синхронизированы заголовки и вехи недели 3 в локалях RU/EN/TR («Небесный Дар / Celestial Gift / Göklerin Hediyesi»).
    - **Ликвидация дублирования интерактивных элементов**:
      * Убрана тесная внутренняя кнопка «Забрать» из карточки дня, заменена на аккуратный статус-бейдж `★ ГОТОВО / READY` с золотой пульсирующей аурой карточки.
      * Устранено дублирование названия вехи в шапке недели на десктопе (`week-milestone-hint`), разгрузив интерфейс до чистого индикатора `[X/7 дней]` и трека прогресса.
    - **Симметрия и Гранд-Финал Недели 4 (дни 22–30)**:
      * Устранена пустота из 5 слотов на десктопе: день 29 занимает 2 колонки, а день 30 («Око Вечности») растянут на 5 колонок в роскошный Гранд-Финал баннер с постоянным бонусом (+50% доход навсегда).
      * На мобильных неделя 4 идеально сбалансирована: дни 22–27 (2 ряда по 3), дни 28 и 29 (симметричный ряд из 2 карточек `span 3`), день 30 — полноэкранный Гранд-Финал баннер.
    - **Эргономика футера и мобильного VIP-баннера**:
      * Когда награда дня забрана, вместо 65px громоздкого блока выводится сверхкомпактная статус-плашка (`✔ Награда дня получена • До следующей: завтра`), освобождающая ценное вертикальное пространство для комфортного скролла всех 30 дней.
      * В VIP-баннере устранены обрезки текста на смартфонах (`э...`), touch target кнопки `[ВИП]` приведен к эталону >=44x44px.
      * Устранено наложение плашек `СУПЕР-НАГРАДА` и `Гранд-Финал` на текст в мобильных карточках благодаря flex-интеграции внутри шапки карточки.

20. **Grand Arcanum & Ascension Ritual Mobile Ergonomics & 60 FPS Engine Overhaul («Великий Арканум и Ритуал Перерождения: Мобильная Эргономика и Аппаратные 60 FPS»)**:
    - **Полная видимость всех 4 вкладок Арканума на смартфонах**:
      * В `PremiumModal.svelte` сетка табов переведена на `grid-template-columns: repeat(4, minmax(0, 1fr))` и добавлен `overflow: hidden;` на ячейки.
      * Введена система дуальных меток табов: на десктопе отображаются полные названия («Сокровищница», «Хрономантия», «Тайные Знания», «Ритуал»), а на экранах `<=680px` включаются адаптивные лаконичные подписи («Казна», «Хроно», «Знания», «Ритуал» в RU; «Treasury», «Chrono», «Secrets», «Ritual» в EN; «Hazine», «Zaman», «Bilgi», «Ritüel» в TR).
      * Исключено выталкивание 4-й вкладки перерождения за пределы экрана: все 4 вкладки идеально сбалансированы на экранах шириной от 320px до 390px с touch target >=44px и аккуратными статус-бейджами.
    - **Ликвидация SMIL и оптимизация 60 FPS в RebirthModal**:
      * Удалены тяжелые SVG-теги `<animateTransform>` и `<animate>`, а также фильтр `<feGaussianBlur>`, вызывавшие просадки кадров на мобильных устройствах.
      * Все анимации ритуальной печати (вращение концентрических колец по часовой и против часовой стрелки, пульсация рунического ядра) переведены на аппаратные CSS-кейфреймы `@keyframes` (`transform: rotate`, `will-change: transform`).
    - **Адаптивная компоновка Ритуала Перерождения**:
      * Секция печати `.seal-section` на мобильных компактно центрируется: масштабированный SVG-символ, крупный блок награды Звёздной Пыли и прогресс-бар до следующей единицы пыли.
      * Карточки «Приносится в Жертву» и «Сохраняется Навсегда» в `.comparison-grid` переведены на 1 колонку (`grid-template-columns: 1fr`) со 100% шириной, исключив обрезку текста.
      * Главная CTA-кнопка ритуала перерождения дополнена безопасным переносом текста (`white-space: normal`) и высотой >=48px.
    - **Полировка карточек в SecretUpgradesTab**:
      * Устранена поломка вёрстки с растягиванием кнопок покупки на 100% ширины на смартфонах: сохранена эргономичная горизонтальная структура (иконка 40x40, инфо с тегами и прогресс-баром, компактная кнопка покупки справа).
      * Стандартизированы медиа-запросы к общему брейкпоинту `@media (max-width: 680px)` и под-брейкпоинту `@media (max-width: 380px)`.

21. **Visual & Ergonomics Bugfixes Overhaul (Арканум и Инвентарь Зелий)**:
    - **Ликвидация срезания баланса в Великом Аркануме (`PremiumModal.svelte`)**:
      * На `.master-header` добавлен `flex-shrink: 0;` (и в базовых стилях, и в мобильных `@media`), устранив сжатие шапки флексбоксом модалки и вернув полную видимость чипов ресурсов (золото, кристаллы, звёздная пыль) на десктопе.
      * На `.tab-content-area` задан `flex: 1 1 auto; min-height: 0;`.
    - **Эргономика и ликвидация обрезки бейджей табов Арканума**:
      * С `.arcanum-tab` удален деструктивный `overflow: hidden;`, исключив обрезку верхней границы и теней бейджей (`ДАР`, `1 Ч БЕСП`).
      * Внедрены дуальные метки для бейджа ускорения: на десктопе отображается `1 Ч БЕСП`, а на мобильных устройствах — компактный `1 Ч` (`1H` / `1S`), не перекрывающий иконку песочных часов.
      * Аналогично бейдж VIP адаптирован: `+10 ВИП` на десктопе и `+10` на смартфонах.
    - **Гармонизация скроллбара и отключение двойных скроллов**:
      * Скроллбар `.tab-content-area` переведен с ядовито-желтого цвета на лавандово-золотую палитру проекта (5px, прозрачный трек, скругленный ползунок `rgba(162, 155, 254, 0.25)`).
      * Во вложенных модалках (`BankModal.svelte`, `CrystalShopModal.svelte`) для `.embedded-modal` добавлено `overflow: visible; overflow-y: visible; height: auto;`, устранив паразитные двойные скроллбары.
    - **Радикальная переработка карточки зелий в Инвентаре (`InventoryModal.svelte`)**:
      * Ликвидировано слипание цифр мастерства и кнопок продажи (`★ Мастерство: Ур. 0/100/1 выпито`): карточка зелья переведена на просторную 3-уровневую структуру:
        1. Верхний ряд: иконка 44x44 + название с бейджем количества и описанием + кнопка «✓ Пить» (CTA с touch target >=38px);
        2. Блок мастерства: развернут на 100% ширины карточки (гибкий заголовок с `gap: 8px` и `margin-left: auto` для счетчика выпитого, широкий плавный золотой трек прогресса, изумрудный текст бонуса);
        3. Нижний ряд: симметричные кнопки быстрой продажи («Продать 1 шт.» и «Все») в горизонтальный ряд.
    - **Точная посадка бейджа справки в `MechanicHelpButton.svelte`**:
      * Скорректированы координаты `.unread-spark-dot` (`top: -1px; right: -1px;`), исключив выталкивание за пределы круглой кнопки.

22. **Procedural Web Audio Ambient Engine, Sound Palette & Settings Modal («Процедурный Web Audio Эмбиент, Новая Звуковая Палитра и Модалка Настроек»)**:
    - **Непрерывный процедурный Web Audio синтезатор атмосферного эмбиента (`AmbientEngine` в `audio.ts`)**:
      * Реализован чистый процедурный синтез без внешних аудиофайлов (0 КБ сетевого трафика).
      * Звуковой ландшафт: тёплый низкочастотный гул очага котла (сумма синусоиды 65.41 Гц [C2] и треугольной волны 130.81 Гц [C3]), пропущенный через резонансный Lowpass-фильтр со сверхмедленной LFO-модуляцией (0.07 Гц) для эффекта «живого пламени».
      * Эфирный шиммер: мягкая синусоида 523.25 Гц через узкий полосовой фильтр (Bandpass, Q=4.0) с микро-дрожанием фазы, создающая мистическую глубину.
      * Процедурные пузырьки: автономный планировщик каждые 4–8 секунд генерирует случайные тихие лопающиеся пузырьки котла с быстрой модуляцией частоты.
      * Управление воспроизведением: плавный fade-in и fade-out громкости (0.6с линейный рамп без щелчков), автостарт после первого жеста пользователя (`pointerdown`/`keydown`), автоматическая пауза при скрытии вкладки (`visibilitychange`, `blur`, `focus`) и при показе полноэкранной рекламы Яндекса (`muteForAd`/`unmuteAfterAd`).
    - **Расширенная процедурная палитра звуковых эффектов (SFX)**:
      * Шелест страниц гримуара (`playPageTurnSound`): генерация белого шума через полосовой фильтр с динамическим спадом частоты от 950 Гц до 320 Гц и экспоненциальным затуханием (0.18с) — тактильный и реалистичный звук переворачивания пергамента. Интегрирован при смене вкладок Великого Гримуара (`GrimoireModal`), перелистывании рецептов Алхимического стола (`AlchemyTableModal`) и смене языка в Настройках.
      * Звон Звёздной Пыли (`playStardustSound`): мерцающий арпеджио-каскад синусоидальных кристаллов пентатоники (1480, 1760, 2217, 2637 Гц) с мягким спадом. Интегрирован при покупке улучшений в «Тайных Знаниях» (`SecretUpgradesTab`) и совершении Ритуала Перерождения (`RebirthModal`).
    - **Полноценное модальное окно «Настройки» (`SettingsModal.svelte`) и кнопка шестерёнки в HUD**:
      * В шапке приложения добавлен золотой переключатель шестеренки (`settings-btn`) с подсказкой и быстрой клавиатурной доступностью. Поддерживается открытие по URL `?modal=settings`.
      * Раздельное независимое управление звуком: отдельные карточки SFX и Эмбиента с тумблерами-свитчами и ползунками громкости (0–100%) с сохранением в `localStorage`.
      * Быстрый выбор локализации (RU / EN / TR) с вибро-шелестом страниц и мгновенной реакцией интерфейса.
      * Карточка статуса сохранения: индикатор активности облака Яндекс Игр, версия игры и каноничное название игры на каждом языке («Магический магазин» в RU, «Magic Shop Tycoon» в EN, «Büyü Dükkânı» в TR).
      * Дизайн и эргономика: стекломорфизм в темно-фиолетовой гамме со скруглениями 24px, плавные GSAP-анимации, адаптивность под экраны от 320px до 1280px+ с соблюдением touch target >=44x44px.

23. **Elimination of Duplicate HUD Settings Buttons («Устранение дублирующих кнопок настроек в HUD»)**:
    - **Очистка кластера управления верхнего HUD (`App.svelte`)**:
      * Из верхнего HUD удалены устаревшие кнопки переключения языка (`lang-btn`) и быстрого мута звука (`sound-btn`).
      * Управление обоими этими параметрами полностью и бесшовно централизовано в модальном окне «Настройки» (`SettingsModal.svelte`), открываемом через кнопку-шестерёнку.
      * Удален неиспользуемый код-сирота: функции `cycleLanguage()` и `handleToggleSound()`, неиспользуемые импорты `isSoundMuted`, `toggleSound`, а также стили `.hud-icon-btn.sound-btn.muted` и `.lang-label` в медиа-запросах.
      * Результат для мобильного интерфейса: блок кнопок в верхней строке уменьшен с 6 до 4 элементов (`Календарь`, `Колесо Фортуны`, `Лидерборд`, `Настройки`), устранено переполнение и выталкивание кнопок за пределы экрана на смартфонах шириной 360–390px, обеспечены идеальные безопасные отступы и touch target >=38x38px.

24. **Archmage Hall of Fame Achievement System («Зал Славы Архимага: Система Достижений, Титулы и Пассивные Благословения»)**:
    - **Архитектура системы достижений (`src/achievements.ts`)**:
      * Реализовано 8 глобальных направлений по 5 звёздных уровней в каждом (всего 40 уровней / звёзд):
        1. `brew_master` («Мастер Котла»): сварено зелий (10 -> 5,000) -> шанс двойного зелья (+2%..+10%).
        2. `gold_hoarder` («Златой Алхимик»): суммарно заработано золота (10K -> 1B) -> множитель золота (+1%..+5%).
        3. `relic_seeker` («Хранитель Древностей»): открыто реликвий за Звёздную Пыль (1 -> 15) -> множитель золота (+2%..+10%).
        4. `grand_ritual` («Вечный Архимаг»): проведено ритуалов возрождения (1 -> 20) -> множитель Звёздной Пыли (+3%..+15%).
        5. `order_supplier` («Королевский Поставщик»): выполнено заказов горожан (5 -> 250) -> золото за заказы (+5%..+25%).
        6. `beast_whisperer` («Повелитель Фамильяров»): максимальный уровень фамильяра (5 -> 50) -> сила клика котла (+10%..+50%).
        7. `wheel_of_fate` («Избранник Судьбы»): вращений Колеса Фортуны (3 -> 100) -> шанс двойного зелья (+1%..+5%).
        8. `upgrade_master` («Архитектор Лавки»): куплено улучшений лавки (10 -> 200) -> множитель золота (+2%..+10%).
      * Титулы Архимага (6 ступеней от «Ученика Зельевара» до «Великого Архимага Вечности») с прогрессивными цветными аурами и расчетом звёзд до следующего ранга.
      * Накопительные перки суммируются перманентно и динамически усиливают соответствующие формулы игры (`gold_mult`, `stardust_mult`, `double_brew`, `orders_gold`, `click_power`).
    - **Интеграция в Game State и сохранение (`src/store.ts`, `src/yandex-sdk.ts`)**:
      * Добавлены поля `achievements` (Record<string, number>), `totalGoldEarned`, `rebirthCount`, `ordersCompletedCount` с автоматическим сбором прогресса из всех механик (варка, продажа, клики, заказы, ритуалы, реликвии, питомцы, колесо).
      * Реализованы методы `gameStore.claimAchievement(id)` и `gameStore.claimAllAchievements()` с начислением кристаллов и звёздной пыли, а также реактивный производный стор `unclaimedAchievementsCount`.
      * В `yandex-sdk.ts` добавлена безопасная миграция и мерж сохранений со значениями по умолчанию.
    - **Полноэкранное модальное окно «Зал Славы» (`src/components/HallOfFameModal.svelte`)**:
      * Включает карточку титула Архимага с бейджем ранга, счетчиком звёзд и прогресс-баром, панель постоянных пассивных благословений (2-колоночная сетка чипов), баннер «Забрать всё» при наличии готовых наград, и 8 детальных карточек вех со шкалами прогресса, 5 звездами и кнопками «Забрать».
      * Поддержка горячего открытия по URL `?modal=hall`.
      * Полная адаптивность под мобильные и десктопные экраны, идеальный touch target (>=44x44px), отсутствие горизонтального переполнения.
    - **Внутриигровые уведомления-тосты (`src/components/AchievementToast.svelte`) и звуковая фанфара (`src/audio.ts`)**:
      * При выполнении условий новой ступени ачивки на экране появляется анимированный SVG-тост с иконкой, рангом и названием, автоматически скрывающийся через 4.5с с возможностью клика для перехода в Зал Славы.
      * Процедурный Web Audio триумф `playAchievementSound()`: арпеджио мажорного трезвучия с треугольной волной и каскадом кристальных синусоидальных обертонов.
    - **Навигация и локализация (`src/App.svelte`, `ru.ts`, `en.ts`, `tr.ts`)**:
      * В нижнюю консоль навигации (`.master-hub-dock`) добавлен 5-й портал «🏆 Слава» с пульсирующим зеленым бейджем количества готовых наград.
      * Сетка консоли адаптирована с использованием `grid-template-columns: repeat(5, minmax(0, 1fr))`, что полностью исключило горизонтальный дрейф на экранах от 320px до 1280px+.
      * Полная трилингвальная локализация (RU/EN/TR) всех 8 достижений, описаний, титулов, наград и тостов.

---

## 4. Следующие приоритеты
- Контроль долгосрочной экономики: балансировка стоимости ребиртов и поздних улучшений.
- Мониторинг конверсий в Яны и вовлеченности игроков.
