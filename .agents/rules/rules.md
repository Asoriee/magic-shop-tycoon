---
trigger: always_on
---

- Мы разрабатываем игру для Яндекс Игр "Магический магазин". Наша задача изобрести такой продукт, в который будут хотеть играть люди разных возрастов, донатить и приносить большую прибыль мне. При разработки учитывай все требования и пути реализации представленные в Yandex SDK v2. Используй только SVG. Разрабатываем адаптивность на десктоп и мобильное устройство сразу. Локализация на самые популярные языки в комъюнити Яндекс Игр;
- <https://yandex.ru/dev/games/doc/ru/concepts/requirements> придерживайся этим требованиям;
- Я предпочитаю четкие короткие ответы без вырванного контекста и с подробно расписанными действиями. Я хочу, чтобы ты думал и не выдавал быстрые ответы;
- Думай настолько долго, пока не реализуешь то, что хочет пользователь. Пиши красиво и четко, без лишней драмы;
- Мне важна стабильность и максимальная мобильность, придерживайся правилам чистого и совершенного кода;
- Будь со мной честен, не поддакивай мне, старайся общаться максимально по человечески;
- Постоянно анализируй весь наш диалог, а не последние 10 запросов;
- Если я подвергаю твой ответ сомнению, стой на своем мнении, но только тогда, когда я объективно неправ;
- Работаем на удержание игроков и высокий доход;
- Используй русские буквы и символы;
- Всегда доделывай все до конца, не останавливайся на пол пути;
- Делай коммиты сам, когда посчитаешь нужным;
- ЗАПРЕЩЕНО использовать browser_subagent — внутренний браузер IDE зависает и не создаёт сессию нормально. Вместо этого для визуальной проверки использовать Chrome headless через PowerShell по следующему порядку: (1) убедиться что dev-сервер запущен: `try { $r = Invoke-WebRequest -Uri "http://localhost:5173" -UseBasicParsing -TimeoutSec 5; Write-Host $r.StatusCode } catch { Write-Host "NOT_RUNNING" }` — должно вернуть 200; (2) сделать скриншот командой: `& "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless=new --screenshot="C:\projects\magic-shop-tycoon\screenshot.png" --window-size=1280,800 --disable-gpu http://localhost:5173; Start-Sleep -Seconds 3`; (3) просмотреть результат через `view_file` на `C:\projects\magic-shop-tycoon\screenshot.png`; (4) после просмотра удалить: `Remove-Item "C:\projects\magic-shop-tycoon\screenshot.png"`;
- Следи за полной экономикой, игра не должна быть быстрой, рост прогресса при упорном труде;
- Мощная оптимизация для слабых пк и мобильных устройств!;
- Веди дневник по запоминанию инфориации проекта, если он переполняется, очищай ненужное или оптимизируй информацию. Он тебе нужен для быстрого доступа в курс разработки;
